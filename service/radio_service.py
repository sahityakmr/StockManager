def radio_action(adminVar):
    # selection = "You selected the option " + str(adminVar)
    # print(selection)
    if adminVar=="1":
        selection = "You selected the option " + str(adminVar)
        print(selection)
        return
    if adminVar=="2":
        selection = "You selected the option " + str(adminVar)
        print(selection)
        return
    else:
        print("error")